frappe.provide('mgen');

// Initialize MGEN namespace
mgen = {
    init() {
        // Add initialization code here
        console.log("MGEN initialized");
    }
};

$(document).ready(function() {
    mgen.init();
});
